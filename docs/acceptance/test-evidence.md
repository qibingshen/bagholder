# T123 全量测试证据

## 记录范围

本记录对应 T123，用于保存当前版本的全量契约、属性、集成、失败和单元测试结果，以及格式、静态检查、中文规范和空白检查结果。该证据只说明本地实现与当前规格、计划、任务和契约一致，不代表任何真实数据源授权、真实行情正确性、投资收益或交易能力。

固定风险提示：研究参考，不构成投资建议。

## 执行环境

- 操作系统：Windows
- Python：3.12.4
- pytest：8.4.2
- 当前日期：2026-07-16

## 指定命令与结果

| 命令 | 结果 |
| --- | --- |
| `py -3.12 -m pytest` | 通过，`650 passed in 12.59s`，总覆盖率 `88.53%`，满足 `80.0%` 覆盖率门槛 |
| `py -3.12 -m ruff format --check src tests` | 通过，`175 files already formatted` |
| `py -3.12 -m ruff check src tests` | 通过，`All checks passed!` |
| `py -3.12 tools/check_chinese_project_text.py` | 通过，退出码 0 |
| `git diff --check` | 通过，退出码 0 |

## 覆盖范围

本次全量测试覆盖以下类别：

- 契约测试：行情、预测、板块、MCP、模型、备份、桌面状态、报告任务和多市场适配器。
- 属性测试：新鲜度、市场规则、调度、模型发布门禁、时间点完整性、预测标签、板块成员历史、存储预算和时间序列回测。
- 失败场景测试：行情缺失和过期、预测安全、凭据暴露、后台任务恢复、备份恢复、模型治理失败、MCP 数字溯源、不可成交约束和工作进程隔离。
- 集成测试：本地历史行情闭环、市场控制器、预测服务、板块服务、版本化原子提交、存储适配器、回测仓库和新浪行情溯源。
- 单元测试：中文规范、本地服务、可观测性、SQLite 设置和任务状态机。

## 验收结论

当前全量质量门禁通过。后续任何影响行情、预测、MCP、桌面状态、模型治理、备份恢复、跨市场适配器、中文规范或任务状态的变更，都必须重新运行本记录中的命令并更新版本化证据。

# US2 测试结果记录

## 记录范围

本记录对应 T057，用于固化用户故事 2“获得未来 1 日、5 日、20 日上涨、震荡、下跌概率、置信度、依据和风险因素”的契约、数据和失败场景测试结果。该记录只证明本地测试快照，不代表任何真实投资收益，也不得替代后续数据源、模型或界面变更后的重新验收。

固定风险提示：研究参考，不构成投资建议。

## 指定测试命令

```powershell
py -3.12 -m pytest -o addopts='' tests/contract/test_prediction_contract.py tests/property/test_prediction_labels.py tests/failure/test_prediction_failures.py -q
```

## 执行结果

| 类别 | 文件 | 覆盖重点 | 当前结果 |
| --- | --- | --- | --- |
| 契约测试 | `tests/contract/test_prediction_contract.py` | 预测输入输出、概率字段、置信度、依据、风险因素、数据新鲜度、模型版本、固定免责声明和数字溯源 | 通过 |
| 数据测试 | `tests/property/test_prediction_labels.py` | 1 日、5 日、20 日周期，±1%、±3%、±6% 上涨/震荡/下跌标签边界，概率容差与校验规则 | 通过 |
| 失败场景测试 | `tests/failure/test_prediction_failures.py` | 过期或缺失数据阻断、无有效到期价格、版本回写拒绝、不可变预测快照和未来数据泄漏防护 | 通过 |

本次执行输出：`94 passed in 1.44s`，命令退出码为 0。

## 安全边界确认

- 预测输出只能来自本地结构化数据、特征快照、模型版本和可追溯事实引用，不允许大模型补造行情或预测数字。
- 任何特征、标签、回测或到期结果只能使用预测时点已经可获得的数据，必须防止未来数据泄漏。
- 预测快照为追加保存；已经生成的预测不得静默覆盖，修订必须产生带来源的新版本。
- 延迟、过期、缺失或授权不足的数据不得伪装为实时数据，也不得继续生成当前预测。
- 所有预测界面、报告和对话解释必须显示“研究参考，不构成投资建议”。

## 复验要求

当预测标签定义、概率输出格式、特征快照、模型版本、数据新鲜度、到期结果回填、历史预测展示或 MCP 数字溯源发生变更时，必须重新运行本记录中的指定测试，并同步刷新本文件的执行日期、命令和结果。若任一测试失败，不得进入下一用户故事实现。

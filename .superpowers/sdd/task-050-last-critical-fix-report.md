# T050 最后严重门禁修复报告

## 修复范围

- 到期事实新增参考日与到期日两端的签名可交易状态；缺失、停牌或不可成交均只能形成 `PENDING_VALIDATION`。
- 参考价和到期价的事实市场日期按证券所属市场时区严格匹配各自交易日。
- 本地签发方仅从应用内受信注册表获取，解析入口不再接受调用方注入签发方。
- 快照仓储在持久化前重验签名、类型、哈希、价格、市场日期、日历、规则、公司行动及由事实推导的标签，拒绝手工构造的 `VALIDATED` 结果。
- 迁移标签与失败场景的有效事实夹具，使用受信签名、可交易事实和正确市场交易日。

## TDD 证据

新增回归测试先失败：缺可交易事实、直接伪造已验证结果、错误价格市场日期和自注入签发方共 5 项均暴露原门禁缺口；实施后全部通过。

## 验证结果

- `tests/failure/test_prediction_security_regressions.py`：21 通过。
- `tests/property/test_prediction_labels.py`：26 通过。
- `tests/failure/test_prediction_failures.py`：24 通过。
- Ruff 格式与静态检查：通过。
- `git diff --check`：通过。

尚未运行全量测试；未暂存、未提交。
